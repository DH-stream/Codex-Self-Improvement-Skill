# Repository agent instructions

1. Read `CORE.md` first.
2. For skill or procedure changes, use `superpowers:writing-skills`.
3. For multi-step changes, use `superpowers:writing-plans`.
4. Treat `tests/baseline-observations.md` as the initial RED evidence.
5. Keep `skills/codex-self-improvement/SKILL.md` compact; move detail to `references/`.
6. Never overwrite or delete learned evidence.
7. After any prompt changing skill, workflow, installer, test, or memory schema files, run the self-improvement post-change procedure.
8. A user correction or blocker follow-up requires the correction retrospective.
9. Do not weaken quality gates to optimize tokens.
10. Update `memory/UPDATE_LOG.md` for material engine or seed-memory changes.
